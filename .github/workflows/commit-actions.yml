name: commit-actions
on: [push]
jobs:
  gradle-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java
      with:
        java-version: '21'
        